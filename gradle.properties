# 1. 内存控制在 1.2G ~ 1.5G 之间，防止触发宿主机 Swap 和 OOM
org.gradle.jvmargs=-Xmx1280m -XX:MaxMetaspaceSize=384m -XX:+UseG1GC

# 2. 必须开启 Daemon（常驻内存避免频繁冷启动拉高 CPU）
org.gradle.daemon=true

# 3. 建议关闭并行编译（容器太多了，关闭并行编译可以避免 CPU 瞬间爆满）
org.gradle.parallel=false

# 4. 保持构建缓存
org.gradle.caching=true